RSpec.describe Rails::Gdpr::Export do
  it "has a version number" do
    expect(Rails::Gdpr::Export::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
